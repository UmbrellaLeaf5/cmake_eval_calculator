# language: CMake

# Set the project directory to the current source directory
cmake_path(SET PROJECT_DIR "${CMAKE_CURRENT_SOURCE_DIR}")

# Set the main directory for the CMake files
cmake_path(SET MAIN_DIR "${PROJECT_DIR}/main")
